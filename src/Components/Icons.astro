---
import { IconMap, type IconCode } from '../store/icons';

interface Props {
  iconCode: IconCode;
}

const { iconCode } = Astro.props as Props;
---

<span class={IconMap[iconCode]} role="img" aria-hidden="true" data-icon={iconCode} />